# 管理后台开发环境 Dockerfile
# 支持 Vite HMR 热重载

FROM node:20-alpine

WORKDIR /app

# 复制 package 文件
COPY package.json package-lock.json* ./

# 安装依赖
RUN npm install && npm cache clean --force

# 暴露端口
EXPOSE 3001

# 开发模式启动 - --host 允许 Docker 外部访问
CMD ["npm", "run", "dev", "--", "--host"]