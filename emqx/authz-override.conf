# EMQX 认证配置覆盖
# 此文件将被加载到EMQX中

# 认证器配置
authentication {
  backend = built_in_database
  mechanism = password_based
  password_hash_algorithm {
    name = plain
  }
  user_id_type = username
}

# 内置用户数据（初始用户）
# 用户: test1, 密码: test123
# 注意：用户数据会在Dashboard中管理，这里只是占位配置